# Optional host-side diagnostic port.
# v79_nginx_proxy does NOT use this port; it connects to gaming-studio-j:80
# directly over the shared proxy_network.
GAMES_PORT=8095

# Keep the host port local-only when using v79_nginx_proxy.
GAMES_BIND_ADDRESS=127.0.0.1
